apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-deploy
  labels:
    app: nginx-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-app
  template:
    metadata:
      labels:
        app: nginx-app
    spec:
      containers:
      - name: nginx-container
        image: nginx:1.17.0
        ports:
        - containerPort: 80
